#include<stdio.h>

int main (){
    int a;
    a = 8;
    printf("%d", a );
    return 0;

}
 //rules for naming variable in c
 // 1 first character must be an alphabet or underscore(_)
 //no special symbol other than (_) allowed
 //variable names are case sensitive


 //CONSTANT
 //an entity whose value does not change is called  as a constant
 //a variable is a entity whose value can be changed