#include<stdio.h>

int main () {
    int a;
    scanf("%d", &a);
    printf("the value of a is %d", a);
     return 0;
}
// %d is for integer
// %f is for float
// %c is for character
//in order to takeinput from the user and assign it to a variable we use scanf() function 
//'&' is the 'address of' operator ND IT MEANS THAT THE SUPPLIED value should be cpied to the address which is indicated by variable i
